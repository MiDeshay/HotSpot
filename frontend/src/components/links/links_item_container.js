import { connect } from "react-redux";
import LinksItem from "./links_item";

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(LinksItem);